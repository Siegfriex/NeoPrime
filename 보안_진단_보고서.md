# Excel 파일 보안 진단 보고서

**진단 일시**: 2026-01-26 15:26:06  
**진단 도구**: `security_scan.py`  
**진단자**: 데이터 보안 전문가 / Python 자동화 엔지니어

---

## 📋 파일 정보

| 항목 | 내용 |
|------|------|
| **파일명** | `202511고속성장분석기(가채점)20251114.xlsx` |
| **전체 경로** | `Y:\0126\0126\202511고속성장분석기(가채점)20251114.xlsx` |
| **파일 크기** | 23.36 MB (24,498,999 bytes) |
| **수정 일시** | 2026-01-26 15:05:54 |
| **파일 형식** | Excel Workbook (.xlsx) |

---

## 🔒 1. 파일 접근성

### 진단 결과
- **상태**: ⚠️ **[부분 제한]** (암호화 감지됨, 하지만 openpyxl로 읽기 가능)

### 상세 분석

#### Level 1: 파일 암호 확인
- **msoffcrypto-tool 진단 결과**: 파일이 암호화되어 있음 - 비밀번호 필요
- **오류 메시지**: `No key specified`
- **openpyxl 테스트 결과**: ✅ 파일 열기 성공

#### 해석
파일이 암호화되어 있지만, 암호화 방식이 완전하지 않거나 특정 방식으로 인해 `openpyxl` 및 `Pandas` 라이브러리로는 비밀번호 없이도 읽을 수 있습니다.

이는 다음과 같은 경우에 발생할 수 있습니다:
- 파일 열기 암호화만 적용되고 쓰기 암호화는 적용되지 않은 경우
- Excel의 특정 암호화 방식이 openpyxl에서 우회 가능한 경우
- 암호화 키가 파일 내부에 포함되어 있는 경우

### 권장 사항
✅ **Pandas로 직접 읽기 시도 가능**  
- 비밀번호 없이도 데이터 추출이 가능합니다.
- `pd.read_excel()` 함수를 사용하여 시도해볼 수 있습니다.

---

## 🛡️ 2. 시트 보호 현황

### 진단 결과
- **보호된 시트**: **0개**
- **보호되지 않은 시트**: **15개**
- **총 시트 수**: **15개**

### 데이터 추출 가능 여부
✅ **[가능]**

### 상세 설명
모든 시트가 보호되지 않은 상태입니다. 시트 보호가 적용되어 있더라도 Pandas는 시트 보호를 무시하고 데이터를 읽을 수 있습니다.

**참고**: Pandas의 `read_excel()` 함수는 시트 보호 설정과 무관하게 셀의 데이터를 읽을 수 있습니다. 시트 보호는 Excel 애플리케이션에서의 수정을 방지하는 것이지, 파일 자체의 데이터 접근을 막는 것은 아닙니다.

---

## 👁️ 3. 숨겨진 시트(Source)

### 진단 결과
- **보이는 시트**: **15개**
- **숨김 시트**: **0개**
- **완전숨김 시트**: **0개**

### 상세 분석
모든 시트가 보이는 상태(`visible`)입니다. 입시 분석 툴의 특성상 원본 데이터가 숨겨진 시트에 있을 가능성이 높지만, 이 파일에서는 숨겨진 시트가 발견되지 않았습니다.

### 시트 상태 분류
- **visible**: Excel에서 정상적으로 보이는 시트
- **hidden**: Excel에서 숨김 처리된 시트 (시트 탭에서 우클릭으로 다시 보이게 할 수 있음)
- **veryHidden**: Excel에서 완전히 숨김 처리된 시트 (VBA를 통해서만 접근 가능)

---

## ✅ 4. 최종 결론

### 판정 결과
✅ **이 파일은 Pandas로 분석 가능합니다 (암호화가 완전하지 않음).**

### 종합 평가

| 항목 | 상태 | 비고 |
|------|------|------|
| **파일 암호화** | ⚠️ 감지됨 | 하지만 openpyxl/Pandas로 읽기 가능 |
| **시트 보호** | ✅ 없음 | 보호된 시트 0개 |
| **숨김 시트** | ✅ 없음 | 모든 시트가 보이는 상태 |
| **데이터 추출** | ✅ **가능** | Pandas로 즉시 분석 가능 |

### 권장 작업 절차

1. **즉시 분석 가능**
   ```python
   import pandas as pd
   
   # 파일 읽기 시도
   df = pd.read_excel('Y:\\0126\\0126\\202511고속성장분석기(가채점)20251114.xlsx')
   
   # 또는 특정 시트 읽기
   df = pd.read_excel('Y:\\0126\\0126\\202511고속성장분석기(가채점)20251114.xlsx', 
                      sheet_name='시트명')
   ```

2. **시트 목록 확인**
   ```python
   import pandas as pd
   
   # 모든 시트 목록 가져오기
   excel_file = pd.ExcelFile('Y:\\0126\\0126\\202511고속성장분석기(가채점)20251114.xlsx')
   print(excel_file.sheet_names)
   ```

3. **전체 시트 읽기**
   ```python
   import pandas as pd
   
   # 모든 시트를 딕셔너리로 읽기
   all_sheets = pd.read_excel('Y:\\0126\\0126\\202511고속성장분석기(가채점)20251114.xlsx', 
                               sheet_name=None)
   
   # 각 시트별로 처리
   for sheet_name, df in all_sheets.items():
       print(f"시트: {sheet_name}, 행 수: {len(df)}")
   ```

### 주의사항

⚠️ **파일이 암호화되어 있다는 점을 고려하여**:
- 데이터 추출은 가능하지만, 파일의 원본 보안 정책을 존중해야 합니다.
- 추출한 데이터의 사용 목적과 권한을 확인하세요.
- 민감한 정보가 포함되어 있을 수 있으므로 데이터 보안에 주의하세요.

---

## 🔧 사용된 도구 및 환경

### 진단 도구
- **스크립트**: `security_scan.py`
- **Python 버전**: Python 3.14.2
- **사용 라이브러리**:
  - `msoffcrypto-tool` 6.0.0 (파일 암호 확인)
  - `openpyxl` 3.1.5 (시트 보호 및 숨김 시트 확인)

### 진단 방법론
1. **Level 1**: `msoffcrypto-tool`을 사용한 파일 암호화 여부 확인
2. **Level 2**: `openpyxl`을 사용한 시트 보호 상태 확인
3. **Level 3**: `openpyxl`을 사용한 숨김 시트 확인

---

## 📊 부록: 시트 목록

총 **15개**의 시트가 확인되었습니다. 모든 시트는 보호되지 않았으며, 숨김 처리도 되어 있지 않습니다.

시트 목록을 확인하려면 다음 코드를 실행하세요:

```python
import pandas as pd

excel_file = pd.ExcelFile('Y:\\0126\\0126\\202511고속성장분석기(가채점)20251114.xlsx')
for i, sheet_name in enumerate(excel_file.sheet_names, 1):
    print(f"{i}. {sheet_name}")
```

---

## 📝 보고서 생성 정보

- **생성 일시**: 2026-01-26 15:26:06
- **진단 소요 시간**: 약 30초
- **보고서 형식**: Markdown (.md)
- **다음 진단 권장 시기**: 파일이 업데이트되거나 보안 설정이 변경된 경우

---

**보고서 끝**
